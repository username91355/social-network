import React from 'react';
import preloader from '../../../assets/gif/loading.gif'

const Preloader = () => {
    return <img style={{width: '50px'}} src={preloader} alt={'loading'}/>
};

export default Preloader;